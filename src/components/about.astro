<section id="about">
  <div class="about-container">
    <h2>&Uuml;ber mich</h2>
    <div class="about-content">
      <div class="about-text">
        <p>Hallo! Ich bin Jonas, ein leidenschaftlicher Web Developer und Designer aus &Ouml;sterreich. Mit &uuml;ber 3 Jahren Erfahrung in der Web-Entwicklung helfe ich Unternehmen, ihre digitalen Ziele zu erreichen.</p>
        <div class="skills">
          <h3>Skills</h3>
          <div class="skill-tags">
            <span class="skill-tag">HTML/CSS</span>
            <span class="skill-tag">JavaScript</span>
            <span class="skill-tag">React</span>
            <span class="skill-tag">TypeScript</span>
            <span class="skill-tag">Java</span>
            <span class="skill-tag">C#</span>
            <span class="skill-tag">C</span>
            <span class="skill-tag">Unreal Engine 5</span>
            <span class="skill-tag">UI Design</span>
            <span class="skill-tag">Git</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
#about {
  padding: 6rem 2rem;
  background: var(--bg-dark);
  border-top: 1px solid var(--border-color);
  border-bottom: 1px solid var(--border-color);
}

.about-container {
  max-width: 1200px;
  margin: 0 auto;
}

.about-content {
  display: block;
}

.about-text p {
  font-size: 1.1rem;
  line-height: 1.8;
  margin-bottom: 2rem;
}

.skills {
  margin-top: 2rem;
}

.skills h3 {
  margin-bottom: 1.5rem;
  color: var(--text-primary);
}

.skill-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 0.8rem;
}

.skill-tag {
  padding: 0.6rem 1.2rem;
  border-radius: 8px;
  background: var(--card-bg);
  border: 1px solid var(--border-color);
  color: var(--text-secondary);
  font-size: 0.9rem;
  font-weight: 500;
  transition: var(--transition);
}

.skill-tag:hover {
  background: rgba(255, 255, 255, 0.08);
  border-color: var(--text-secondary);
  transform: translateY(-2px);
}

@media (max-width: 768px) {
  .skill-tags {
    gap: 0.5rem;
  }

  .skill-tag {
    padding: 0.5rem 1rem;
    font-size: 0.8rem;
  }
}
</style>
