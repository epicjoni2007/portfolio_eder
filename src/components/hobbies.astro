<section id="hobbies">
  <div class="hobbies-container">
    <h2>Hobbys</h2>
    <div class="hobby-tags">
      <span class="hobby-tag">Unihockey</span>
      <span class="hobby-tag">Beachvolleyball</span>
       <span class="hobby-tag">SUP</span>
      <span class="hobby-tag">Fitnessstudio</span>
    </div>
  </div>
</section>

<style>
#hobbies {
  padding: 3rem 2rem;
  background: var(--bg-dark);
  border-top: 1px solid var(--border-color);
}

.hobbies-container {
  max-width: 900px;
  margin: 0 auto;
}

.hobby-tags {
  display: flex;
  gap: 0.6rem;
  flex-wrap: wrap;
  margin-top: 0.75rem;
}

.hobby-tag {
  padding: 0.45rem 1rem;
  background: rgba(255,255,255,0.03);
  border: 1px solid var(--border-color);
  color: var(--text-secondary);
  border-radius: 999px;
  font-size: 0.95rem;
  font-weight: 600;
}
</style>